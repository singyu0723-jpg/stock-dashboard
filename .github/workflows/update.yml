name: 每日盤後更新 data.json

on:
  schedule:
    - cron: '0 9 * * 1-5'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: 抓官方資料產生 data.json
        run: python update_data.py
      - name: 提交變更
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data.json
          git commit -m "auto: 盤後更新 $(TZ=Asia/Taipei date +%Y-%m-%d)" || echo "無變更"
          git push
